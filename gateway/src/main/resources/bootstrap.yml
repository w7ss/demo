spring:
  application:
    name: gateway
  profiles:
    active: native

  cloud:
    config:
      fail-fast: true
      name: ${spring.application.name}
      #profile: ${spring.profiles.active}
      discovery:
        enabled: true
        service-id: config

# ×¢²áÖĞĞÄÅäÖÃ
eureka:
  client:
    service-url:
      defaultZone: http://eureka1:8080/eureka/

